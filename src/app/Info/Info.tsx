import styles from "./Info.module.css"

export const Info = () => {
    return (
        <>

            <div className={`sectionComponent ${styles.Info}`}>

                INFO HERE
            </div>
        </>
    );
};

export default Info